services:
  currency-mt-parser:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: currency-mt-parser
    ports:
      - "8080:8080"
    environment:
      APP_COLLECTION_INTERVAL_SECONDS: 300
