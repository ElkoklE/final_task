apiVersion: 1

providers:
  - name: 'currency-performance'
    orgId: 1
    folder: 'Currency MT Parser'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
